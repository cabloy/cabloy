$(document).ready(function() {
  // echo
  util.article.echo();
  // breadcrumb
  util.article.breadcrumb({container:'.breadcrumb-nav'});
  // relatives
  util.article.relatives({container:'.relatives'});
});

$(document).on('echo-ready', function() {
  // stats
  util.article.stats();
  // comments
  util.article.comments({container:'.comments'});
  // recent comments
  util.article.commentsRecent({container:'.comments-recent'});
});
