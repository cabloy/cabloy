$(document).ready(function() {
  // echo
  util.article.echo();
  // breadcrumb
  util.article.breadcrumb();
  // relatives
  util.article.relatives();
});

$(document).on('echo-ready', function() {
  // stats
  util.article.stats();
});
