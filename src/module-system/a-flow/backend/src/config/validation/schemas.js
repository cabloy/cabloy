module.exports = app => {
  const schemas = {};
  // flowDefinition
  schemas.flowDefinition = {
    type: 'object',
    meta: {
      custom: {
        // component: 'flowDefinitionItem',
      },
    },
    properties: {
      atomName: {
        type: 'string',
        ebType: 'text',
        ebTitle: 'Name',
        notEmpty: true,
      },
      description: {
        type: 'string',
        ebType: 'text',
        ebTitle: 'Description',
      },
    },
  };
  // flowDefinition search
  schemas.flowDefinitionSearch = {
    type: 'object',
    properties: {
      description: {
        type: 'string',
        ebType: 'text',
        ebTitle: 'Description',
      },
    },
  };
  return schemas;
};
