// eslint-disable-next-line
module.exports = appInfo => {
  const config = {};

  // broadcasts
  config.broadcasts = {
    dictCacheRemove: {
      bean: 'dictCacheRemove',
    },
  };

  return config;
};
