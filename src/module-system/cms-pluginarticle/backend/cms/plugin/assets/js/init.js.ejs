$(document).ready(function() {
});

$(document).on('echo-ready', function() {
  // stats
  util.article.stats();
  // brothers
  util.article.brothers({container:'.brothers'});
  // comments
  util.article.comments({container:'.comments'});
  // article edit
  util.article.articleEdit({container:'.article-edit'});
});
