<%
// categories
const categories = await ctx.meta.util.performAction({
  method: 'post',
  url: '/a/base/category/tree',
  body: {
    atomClass: site.atomClass,
    language: site.language && site.language.current,
    categoryId: 0,
    categoryHidden: 0,
  },
});
// output
const js=`var _categories=${JSON.stringify(categories)};`;
echo(js);
%>
