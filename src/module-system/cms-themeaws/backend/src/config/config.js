// eslint-disable-next-line
module.exports = appInfo => {
  const config = {};

  // theme
  config.theme = {
    _theme: {
      name: 'cms-themeaws',
      url: 'https://www.npmjs.com/package/egg-born-module-cms-themeaws',
    },
  };

  return config;
};
