// eslint-disable-next-line
module.exports = appInfo => {
  const config = {};

  // plugin
  config.plugin = {
    track: {
      google: '',
      baidu: '',
      qq: '',
    },
  };

  return config;
};
