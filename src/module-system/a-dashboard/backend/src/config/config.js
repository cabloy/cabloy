// eslint-disable-next-line
module.exports = appInfo => {
  const config = {};

  // dashboard
  config.dashboard = {
    default: 'a-dashboard:default',
    home: 'a-dashboard:home',
  };

  return config;
};
