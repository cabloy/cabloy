{
  "name": "egg-born-module-test-localeone",
  "version": "4.0.5",
  "title": "Locale One",
  "eggBornModule": {
    "fileVersion": 0,
    "locale": "en-us,zh-cn",
    "dependencies": {
      "a-instance": "4.0.0",
      "a-base": "4.0.0"
    }
  },
  "description": "cabloy locale test",
  "keywords": [
    "Cabloy Module"
  ],
  "scripts": {
    "build:front": "egg-born-bin front-build-module",
    "build:backend": "egg-born-bin backend-build-module",
    "lint": "eslint front backend",
    "preversion": "npm run build:front && npm run build:backend && git add ."
  },
  "author": "zhennann",
  "dependencies": {
    "egg-born-bin": "^3.0.5",
    "egg-born-mock": "^2.0.4",
    "extend2": "^1.0.0",
    "require3": "^2.0.4"
  }
}
