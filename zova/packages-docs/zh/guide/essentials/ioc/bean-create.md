# Create Bean

可以通过 Cli 命令或者菜单命令创建 Bean Class。

## 举例：创建Model Bean

在模块 demo-student 中创建一个 Model Bean：`test`

### 1. Cli 命令

```bash
$ zova :create:bean model test --module=demo-student
```

### 2. 菜单命令

::: tip
右键菜单 - [模块路径]: `Zova Create/Model`
:::
