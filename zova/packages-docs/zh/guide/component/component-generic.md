# 泛型组件

Zova 支持泛型组件。只需将已创建的组件转换为泛型组件即可，以`card`组件为例:

## 1. Cli命令

```bash
$ zova :refactor:componentGeneric card --module=demo-student
```

## 2. 菜单命令

::: tip
右键菜单 - [模块路径/src/component/componentName]: `Zova Refactor/Convert to Generic Component`
:::
