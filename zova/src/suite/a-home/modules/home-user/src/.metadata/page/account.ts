import { createZovaComponentPage } from 'zova';

import { ControllerPageAccount } from '../../page/account/controller.jsx';

export const ZPageAccount = createZovaComponentPage(ControllerPageAccount, undefined, undefined);
