import { createZovaComponentPage } from 'zova';

import { ControllerPageLocale } from '../../page/locale/controller.jsx';

export const ZPageLocale = createZovaComponentPage(ControllerPageLocale, undefined, undefined);
